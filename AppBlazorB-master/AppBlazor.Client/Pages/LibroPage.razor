@page "/libro"
@inject NavigationManager navigationManager
<h3>LibroPage</h3>
<button @onclick="navegar" class="btn btn-primary">Navegar a FormLibro</button>
@code {
    private void navegar()
    {
        navigationManager.NavigateTo("/formlibro");
    }
}
